# Runtime dependencies for the Navien NWP500 Home Assistant integration.
#
# These must match custom_components/nwp500/manifest.json, which is the
# single source of truth -- it is what Home Assistant installs. CI enforces
# this via scripts/check_dependency_pins.py.
#
# Bump with: python scripts/update_nwp500_version.py <new-version>
# Development tooling lives in requirements-dev.txt.

# Core integration library
nwp500-python==9.3.1

# AWS IoT SDK for MQTT communication (pulls awscrt)
awsiotsdk==1.31.0
