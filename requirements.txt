# Python dependencies for Navien NWP500 Home Assistant Integration
# These dependencies are also listed in custom_components/nwp500/manifest.json

# Core integration library
nwp500-python==3.1.0

# AWS IoT SDK for MQTT communication
awsiotsdk>=1.25.0

# Development dependencies
# These are used for testing and type checking but not required at runtime
mypy>=1.0.0
homeassistant>=2024.1.0
tox>=4.0.0
